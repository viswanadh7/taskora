import { StyleSheet } from 'react-native';

export const buttons = StyleSheet.create({
    clear: {
        borderWidth: 1.5,
        borderColor: '#244f8f',
        borderRadius: 8,
        height: 40,
        width: 100,
        alignItems: 'center',
        justifyContent: 'center',
    },
    lock: {
        borderWidth: 1.5,
        borderColor: '#244f8f',
        borderRadius: 8,
        height: 40,
        width: 100,
        alignItems: 'center',
        justifyContent: 'center',
    },
    delete: {
        borderWidth: 1.5,
        borderColor: '#ff0000',
        borderRadius: 8,
        height: 40,
        width: 100,
        alignItems: 'center',
        justifyContent: 'center',
    },
});
