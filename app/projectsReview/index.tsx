import React from 'react';
import { Text, View } from 'react-native';

const index = () => {
    return (
        <View>
            <Text>Projects inside page</Text>
        </View>
    );
};

export default index;
